v2.3.0: Add Always-On Display (AOD) lockscreen dim mode. Re-shows a minimal low-power timer view (pure-black AMOLED, dim text, no animations, burn-in drift) after the power button turns the screen off during a study session. Adds JS bridge + screen on/off receiver + dim wake lock + brightness control (Android), an AOD settings toggle, and version bump to 2.3.0.
