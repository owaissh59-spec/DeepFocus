v2.3.2: Fix stale UI after updates while preserving data. Root cause was a cache-first service worker with a never-invalidated cache name that kept serving old assets across updates. Rewrote sw.js to network-first with a version-stamped cache that purges old versions; version the SW registration by app version and auto-reload the PWA on update; on desktop (Tauri) unregister stale service workers and clear Cache Storage only (IndexedDB and localStorage are untouched, so study history and settings are retained). Also shrink the WebView2 HTTP cache via additionalBrowserArgs and add no-cache meta tags. Bumps versions to 2.3.2 (Android versionCode 6).
